# GENERATED by stayturgid site-sync — DO NOT EDIT (drift → site-sync exit 2)
# template: control/site_contract/sync_templates/fragments/olivetin/stayturgid_actions.yaml.j2
# product_version: 2.7
#
# OliveTin action projection INPUT (design §2 / site-contract §5.3) — not the
# live config.yaml. site-sync merges this file with the optional site file
# <site_dir>/olivetin/user-actions.yaml into ~/.config/<site_ns>/olivetin/
# config.yaml (single writer). Action ids here must be stayturgid_-prefixed;
# user actions must be user_-prefixed; any other prefix or duplicate id → exit 1.
#
# Pure projection of (inventory, registry): hosts sorted by name, no probes,
# no timestamps. OliveTin runs actions in a clean launchd shell — every shell
# block exports PATH explicitly (ovgo plan §Phase-3 env-propagation warning).

actions:
  - id: stayturgid_fleet_health
    title: "Fleet health (just health)"
    icon: "&#129658;"
    timeout: 300
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just health
  - id: stayturgid_fleet_deploy_check
    title: "Deploy check — all hosts (dry-run)"
    icon: "&#128269;"
    timeout: 1800
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just deploy-check
  - id: stayturgid_fleet_deploy
    title: "Deploy fleet — all hosts"
    icon: "&#128640;"
    timeout: 3600
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just deploy

  - id: stayturgid_deploy_hd8
    title: "Deploy Kindle Fire HD 8 (hd8)"
    icon: "&#128640;"
    timeout: 1800
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just deploy hd8

  - id: stayturgid_deploy_mac
    title: "Deploy mac (mac)"
    icon: "&#128640;"
    timeout: 1800
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just deploy mac

  - id: stayturgid_deploy_mac_mini_intel
    title: "Deploy mac-mini-intel (mac-mini-intel)"
    icon: "&#128640;"
    timeout: 1800
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just deploy mac-mini-intel

  - id: stayturgid_deploy_p7a
    title: "Deploy Pixel 7a (p7a)"
    icon: "&#128640;"
    timeout: 1800
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just deploy p7a

  - id: stayturgid_deploy_s24
    title: "Deploy Galaxy S24 (s24)"
    icon: "&#128640;"
    timeout: 1800
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just deploy s24

  - id: stayturgid_deploy_vps_primary
    title: "Deploy vps-primary (vps-primary)"
    icon: "&#128640;"
    timeout: 1800
    shell: |
      export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
      cd ${OPS_ROOT:-/Users/djbclark/ops}/stayturgid
      just deploy vps-primary

