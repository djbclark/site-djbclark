---
# Site-owned control-node maintenance LaunchAgents (Phase F1).
- name: Configure site maintenance LaunchAgents
  hosts: localhost
  connection: local
  gather_facts: true
  roles:
    - role: site_agents
