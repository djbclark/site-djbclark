---
# Site-owned Goose Desktop/CLI entry point (Phase E2 provider + E3 MCP extensions).
- name: Configure Goose against local LiteLLM
  hosts: localhost
  connection: local
  gather_facts: true
  roles:
    - role: goose
